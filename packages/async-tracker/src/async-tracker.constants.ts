export const STORE_PREFIX = 'kikar-';
export const SCAN_ID = `${STORE_PREFIX}scan-id` as const;
export const ORG_ID = `${STORE_PREFIX}org-id` as const;
export const REQUEST_ID = `${STORE_PREFIX}request-id` as const;
export const CORRELATION_ID = `${STORE_PREFIX}correlation-id` as const;
